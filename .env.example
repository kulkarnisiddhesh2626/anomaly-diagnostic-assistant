# Copy to .env and fill in. The app auto-loads .env via python-dotenv.
# Leave ANTHROPIC_API_KEY blank to run in offline/template mode (still fully functional).
ANTHROPIC_API_KEY=
# Optional overrides:
ANOMALY_LLM_MODEL=claude-sonnet-4-6
ANOMALY_LLM_MAX_TOKENS=1200
ANOMALY_LLM_TEMPERATURE=0.2
